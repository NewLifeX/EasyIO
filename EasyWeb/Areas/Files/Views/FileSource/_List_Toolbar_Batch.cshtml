@using NewLife.Common;
@using NewLife.Cube
@{
    var set = ViewBag.PageSetting as PageSetting ?? PageSetting.Global;
}
@if (set.EnableSelect && this.Has(PermissionFlags.Update))
{
    <button type="button" class="btn btn-purple btn-sm" data-action="action" data-url="@Url.Action("Scan")" data-fields="keys" disabled>
        立即扫描
    </button>
}